# Simple WMS

Учебное SPA-приложение для управления складом. Полностью работает в браузере на `Vue 3`, хранит данные в `localStorage` и не использует backend.

## Запуск

```bash
npm install
npm run dev
```

## Сборка

```bash
npm run build
```
